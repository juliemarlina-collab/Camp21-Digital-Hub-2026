document.addEventListener('DOMContentLoaded',()=>{const m=document.querySelector('.menu-btn'),n=document.querySelector('.nav-links');if(m&&n)m.onclick=()=>n.classList.toggle('open');const d=new Date('2026-07-13T08:00:00+08:00'),c=document.querySelector('[data-countdown]');if(c){const t=()=>{const x=Math.max(0,d-new Date()),D=Math.floor(x/86400000),H=Math.floor(x%86400000/3600000),M=Math.floor(x%3600000/60000);c.innerHTML=`<strong>${D}</strong> days <strong>${H}</strong> hrs <strong>${M}</strong> min`};t();setInterval(t,60000)}const checks=document.querySelectorAll('[data-readiness-check]'),bar=document.querySelector('[data-readiness-bar]'),lab=document.querySelector('[data-readiness-label]');function u(){if(!checks.length||!bar)return;const done=[...checks].filter(c=>c.checked).length,pct=Math.round(done/checks.length*100);bar.style.width=pct+'%';if(lab)lab.textContent=`${done}/${checks.length} ready`}checks.forEach(c=>c.addEventListener('change',u));u();});