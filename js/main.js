
const menuBtn=document.querySelector('.menu-btn');const navLinks=document.querySelector('#navLinks');if(menuBtn&&navLinks){menuBtn.addEventListener('click',()=>{navLinks.classList.toggle('open');menuBtn.setAttribute('aria-expanded',navLinks.classList.contains('open'))})}
function countdown(){const target=new Date('2026-07-13T08:00:00+08:00').getTime();const now=Date.now();let diff=Math.max(0,target-now);const d=Math.floor(diff/86400000);diff-=d*86400000;const h=Math.floor(diff/3600000);diff-=h*3600000;const m=Math.floor(diff/60000);diff-=m*60000;const s=Math.floor(diff/1000);document.querySelectorAll('[data-countdown]').forEach(el=>el.textContent=`${d} days to Camp21`);const box=document.querySelector('[data-countdown-boxes]');if(box){const vals=[d,h,m,s];box.querySelectorAll('b').forEach((b,i)=>b.textContent=String(vals[i]).padStart(2,'0'))}}
countdown();setInterval(countdown,1000);
